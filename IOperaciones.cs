public interface IOperaciones
{
    double CalcularArea(double baseValue, double altura);
    double CalcularLongitud(double radio);
    double CalcularVolumen(double largo, double ancho, double alto);
    double CalcularPerimetro(double lado);
    double CalcularDiagonal(double lado1, double lado2);
}
